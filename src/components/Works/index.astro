---
import works from "@/data/works";
import Gallery from "./Gallery.astro";
---

<div
  id="works"
  class="relative overflow-hidden bg-white pb-50 md:pb-85 lg:pb-96 xl:pb-160 portrait:lg:pb-120"
>
  <div class="works-gallery relative z-10 w-max -translate-x-20">
    <ul id="works-list" class="works-list flex h-full w-full">
      {works.map((work) => <Gallery work={work} />)}
      {works.map((work) => <Gallery work={work} />)}
    </ul>
  </div>

  <div
    id="loop-text"
    class="absolute bottom-32 z-1 flex w-max md:bottom-56 lg:bottom-50 xl:bottom-80 portrait:lg:bottom-60"
    aria-hidden="true"
  >
    <span
      class="font-en block px-7 text-8xl opacity-15 md:px-10 md:text-9xl lg:text-[240px]"
      >We Are Strategists with Purpose.</span
    >
    <span
      class="font-en block px-7 text-8xl opacity-15 md:px-10 md:text-9xl lg:text-[240px]"
      >We Are Strategists with Purpose.</span
    >
  </div>
</div>

<script>
  import initWorks from "./initWorks";

  initWorks();
</script>
