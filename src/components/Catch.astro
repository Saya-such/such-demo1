---
import cn from "@/libs/cn";

interface Props {
  h1?: boolean;
  scroll?: boolean;
  others?: string;
}

const { h1 = true, scroll = false, others } = Astro.props as Props;

const catchStyle = cn(
  `font-en absolute left-4 font-bold text-[68px] leading-[1.16] z-10 md:left-6 lg:text-7xl xl:text-8xl xl:leading-none 2xl:text-9xl portrait:md:text-[100px] portrait:lg:text-[144px]`,
  {
    "pb-3": !scroll,
  },
  others,
);
---

{
  h1 ? (
    <h1 class={catchStyle}>
      <span class="lg:hidden portrait:lg:block">
        Hype
        <br />
        Meets
        <br />
        Strategy.
      </span>
      <span class="hidden lg:block portrait:lg:hidden">
        Hype Meets Strategy.
      </span>
    </h1>
  ) : (
    <span aria-hidden="true" class={catchStyle}>
      <span class="lg:hidden portrait:lg:block">
        Hype
        <br />
        Meets
        <br />
        Strategy.
      </span>
      <span class="hidden lg:block portrait:lg:hidden">
        Hype Meets Strategy.
      </span>
    </span>
  )
}
